## 1. Home dashboard stat cards

- [x] 1.1 Add `MatCardModule` to `home.ts` imports
- [x] 1.2 Replace 4x `<div class="stat-card">` with `<mat-card><mat-card-content>` in `home.html`
- [x] 1.3 Remove `.stat-card` SCSS rules from `home.scss`

## 2. Patient-detail stats bar

- [x] 2.1 Confirm `MatCardModule` is imported in `patient-detail.ts` (add if missing)
- [x] 2.2 Replace 4x `<div class="stat-card">` with `<mat-card><mat-card-content>` in `patient-detail.html`
- [x] 2.3 Remove `.stat-card` SCSS rules from `patient-detail.scss`

## 3. Verification

- [x] 3.1 Run `npm run build` — no compilation errors
- [x] 3.2 Visually confirm stat cards render correctly in light mode
- [x] 3.3 Visually confirm stat cards render correctly in dark mode
